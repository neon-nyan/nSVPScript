# nSVPScript Script v2.0
# by: neon-nyan
# ----------------------------------------------------------
# 

global smoothfps_params = """
{
    gpuid:""" + String(gpu_id) + """,
    cubic:""" + String(gpu_accel) + """,
    rate:{
        num:""" + String(framerate_num) + """,
        den:""" + String(framerate_den) + """,
        abs:""" + String(framerate_mul) + """
    },
    algo:""" + String(interpolate_mode) + """,
    block:false,
    linear:""" + String(gpu_accel) + """,
    mask:{
        cover:""" + String(mask_cover) + """,
        area:""" + String(mask_area) + """,
        area_sharp:1.0
    },
    scene:{
        mode:3,
        blend:""" + String(blend_frame) + """,
        limits:{
            m1:2400,
            m2:4800,    """ /* m1 * 1.5 */ + """
            scene:7200, """ /* m2 * 1.5 */ + """
            zero:2400,  """ /* same as analyse_params >  main > refine > thsad */ + """
            blocks:60   """ /* How much block threshold in percent have to change */ + """
        },
        luma:3.0
    },
    light:{
        light:20,
        border:16
    }
}
"""