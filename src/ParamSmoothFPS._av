# nSVPScript Script v2.0
# by: neon-nyan
# ----------------------------------------------------------
# 

global smoothfps_params = """
{
    gpuid:""" + String(gpu_id) + """,
    cubic:""" + String(gpu_accel?false:false) + """,
    rate:{
        num:""" + String(framerate_num) + """,
        den:""" + String(framerate_den) + """,
        abs:""" + String(framerate_mul) + """
    },
    algo:""" + String(interpolate_mode) + """,
    block:false,
    mask:{
        cover:""" + String(mask_cover) + """,
        area:""" + String(mask_area) + """,
        area_sharp:2.0
    },
    scene:{
        mode:3,
        blend:""" + String(blend_frame) + """,
        limits:{
            m1:1500,
            m2:2870,
            scene:4500,
            zero:""" + String(Round((coarse_max * 16) / 100)) + """,
            blocks:""" + String(Round(((coarse_max * 4.2) / 100) + lambda)) + """
        },
        luma:3.0
    }
}
"""