# nSVPScript Script v2.0
# by: neon-nyan
# ----------------------------------------------------------
# 

global analyse_params   = """
{
    gpu:""" + String(gpu_accel) + """,
    vectors:3,
    block:{
        w:""" + String(block_w) + """,
        h:""" + String(block_h) + """,
        overlap:""" + String(block_overlap) + """
    },
    main:{
        search:{
            type:""" + String(search_method) + """,
            distance:""" + String(search_method == 3 ? "" : "-") + String(search_range) + """,
            satd:""" + String(satd) + """,
            sort:""" + String(sort) + """,
            coarse:{
                width:1280,
                type:""" + String(search_method) + """,
                distance:""" + String(search_method == 3 ? "" : "-") + String(search_range * 2) + """,
                satd:""" + String(satd_inCoarse) + """,
                trymany:""" + String(trymany) + """,
                bad:{
                    sad:2176,   """ /* thsad + ((thsad * 36) / 100) */ + """
                    range:""" + String(search_method == 3 ? "" : "-") + String(search_range * 3) + """
                }
            }
        }
    },
    penalty:{
        lambda:15,
        plevel:4,
        lsad:11500,
        pnew:80,
        pglobal:80,     """ /* same as pnew */ + """
        pzero:160,      """ /* pnew * 2 */ + """
        pnbour:80,      """ /* same as pnew */ + """
        prev:""" + String(Round((framerate_num / framerate_den) / 2)) + """
        
    },
    refine:[{
        thsad:2400,
        search:{
            type:""" + String(search_method) + """,
            distance:""" + String(search_method == 3 ? "" : "-") + String(search_range / 2) + """, """ /* same as pel */ + """
            satd:""" + String(satd) + """
        },
        penalty:{
            pnew:80
        }
    }],
    special:{delta:1}
}
"""