# nSVPScript Script v2.0
# by: neon-nyan
# ----------------------------------------------------------
# 

global super_params     = """
{
    pel: """ + String(pixel_precision) + """,
    scale: {up: """ + String(pixel_upscale_mode) + """, down: """ + String(pixel_downscale_mode) + """},
    gpu: """ + String(super_gpu_accel) + """,
    full: """ + String(pixel_precision == 1 ? false : true) + """
}
"""