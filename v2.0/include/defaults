/*
* nSVScript         | Licensed under The GNU GPL-3.0
* Copyright © 2018  | SMAKTA IT Club - SMK Negeri 3 Kota Tangerang
*/

# clip variable
input           = default(input, last)

/*

# utilisation variable
debug           = default(debug, "none")
gpu             = default(gpu, true)
gpuid           = default(gpuid, 0)                 # 0 means "Default GPU"

# configuration variable
preset          = default(preset, "high")
framerate       = default(framerate, 60.0)
framerate_den   = default(framerate_den, 2.0)
video_type      = default(video_type, "animation")
level_pass      = default(level_pass, 1)
search_method   = default(search_method, "umh")
search_range_1  = default(search_range_1, 4)
search_range_2  = default(search_range_2, 6)
search_range_3  = default(search_range_3, 24)
satd            = default(satd, true)
satd_level      = default(satd_level, 3)
vector_mask     = default(vector_mask, 100)
vector_area     = default(vector_area, 30)
scene_detect    = default(scene_detect, "strong")
frame_mode      = default(frame_mode, "interlace")
*/